// This component previously relied on a Manus-hosted Google Maps proxy and is
// no longer used anywhere in the site (the Map page renders a static event list).
// Kept as an inert stub so nothing references Manus services. Safe to delete.
export {};
